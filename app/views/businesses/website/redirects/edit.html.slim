- title 'Edit Website Redirect'

= render :partial => 'dashbreadcrumb', :locals => { :fn => 'Manage Website', :fp => :website_root, :sn => 'Redirects', :sp => :website_redirects, :an => ' Edit Redirect'}

.row
  .col-lg-12
    .wrapper.wrapper-content
      .row
        .col-lg-12
          .ibox.float-e-margins
            .ibox-content
              = errors_for @redirect
              = form_for @redirect, url: [@business, :website, @redirect] do |form|
                = render form
