- title = 'Following Companies & Causes'

= render :partial => 'dashbreadcrumb',
  :locals => { :fn => 'Local Connections', :fp => :crm_root,
    :sn => 'Local Networks', :sp => :crm_company_lists,
    :an => 'Following Companies & Causes'}

.wrapper.wrapper-content.animated.fadeInRight
  .row
    .col-lg-12
      .ibox.float-e-margins
        .ibox-content
          .pull-right
            = link_to [:edit, @business, :crm_follower_notifications], class: 'btn btn-sm btn-default' do
              => icon 'check-square-o'
              | Set Email Notifications
          - if @listings.empty?
            .row
              .col-lg-4.col-lg-offset-4
                .p-a-3.text-center
                  = image_tag('https://assets.locable.com/r/thumbnail/09856f82-489e-4ef1-97ad-4aed4413e8d5/Connections%20&%20Community.png', style: 'max-height: 220px', class: 'img-responsive center-block')
                  h2 No companies are following you.
                  p.text-left.m-t-md.strong Grow your Local Networks and invite your Local Connections to Follow you back
                  = link_to "Manage Local Networks", [@business, :crm_company_lists]
          - else
            table.footable.table.table-stripped.datatable
              thead
                tr
                  th
                    | Name
                  th
                    | Status
                  th
                    | Types
                  th
                    | # in Network
              tbody
                = render @listings
