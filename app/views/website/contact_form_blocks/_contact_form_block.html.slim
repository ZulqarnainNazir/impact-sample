= render partial: "website/shared/block_assets"

- @contact_form = ContactForm.find_by(id: contact_form_block.contact_form_widget_id)

- if @contact_form
  = render partial: 'website/contact_form_blocks/iframed_form', locals: { contact_form: @contact_form }
