- cache [sidebar_events_feed_block, sidebar_events_feed_block.cache_sensitive_key(params)] do
  .sidebar-events class=sidebar_events_feed_block.custom_class
    a id=sidebar_events_feed_block.custom_anchor_id
    / TODO: I've commented out the below line as we are no longer using the get_content view helper (its moved to a controller concern). The assumption is that this is no longer being used but if we find an error this will need to be addressed.
    / - posts = get_events(business: sidebar_events_feed_block.business, page: 1, per_page: sidebar_events_feed_block.items_limit)
    = render @posts
    - if sidebar_events_feed_block.link_version == 'link_paginate'
      = paginate posts
    - elsif sidebar_events_feed_block.link?
      p = link_to sidebar_events_feed_block.link_label, sidebar_events_feed_block.link_location, class: 'btn btn-default btn-block', role: 'button', target: (sidebar_events_feed_block.link_target_blank ? :_blank : nil), rel: (sidebar_events_feed_block.link_no_follow ? 'no-follow' : nil)
